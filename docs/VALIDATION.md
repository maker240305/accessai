# 검증 기록 — 2026-09-11

## 자동 검사

- production build: 통과
- TypeScript strict typecheck: 통과
- ESLint: 통과
- Vitest: 43개 (프록시·스키마 18개, DOM 통합 25개)
- Manifest V3 파일 경로·제출 서버 주소·비밀키 패턴 검사: 통과
- 제출 ZIP 생성: 통과
- `proxy/.dev.vars` Git 제외: `git check-ignore`로 확인

자동 테스트는 위험 행동 판별, 오래된 DOM 판단 거절, 입력 이벤트·값 반영, 숨은 비밀번호 제외, 장식용 alt 보존, 스타일·alt 복구, 잘못된 AI 행동 거절, 무료 429 응답에서 재시도·유료 fallback 없음, Side Panel과 확장 탭의 메시지 신뢰 경계를 검사합니다. 자동 테스트의 AI 응답은 모의 데이터입니다.

## 실제 서비스 연결

- Google AI Studio Free Tier 화면 확인
- 저장된 키로 Gemini 실제 HTTP 200 및 한국어 응답 확인
- Cloudflare Worker 배포 및 Secret 등록 성공
- 공개 `/health`: HTTP 200, ready=true
- 프록시를 통한 실제 AI 명령: 로그인 → CLICK(login), 검색 → TYPE(search, 맥북)
- 합성 빨간색 PNG 이미지에 대한 실제 한국어 설명 반환

## 실제 Chrome UI 검증

개발 서버 전용 프런트엔드가 아닌 production MV3 폴더를 Chrome에 로드하여 확인했습니다.

- 설치 및 고정 확장 ID 확인
- 중복 호스트 권한 경고 수정
- Alt+Shift+A로 Side Panel 열기 확인
- QA 페이지에서 이미지 누락 2개, 대비 문제 1개, 작은 글씨 5개 탐지
- 실제 이미지 가져오기 → 프록시 → AI → alt 반영 확인: 이미지 접근성 이름에 빨간색·파란색 설명이 표시됨
- 설명 적용 후 이미지 누락 0개, 글자 개선 후 작은 글씨 0개
- 고대비 적용 후 대비 문제 0개
- 원상 복구 후 이미지 누락 2개·작은 글씨 5개로 돌아옴
- 재적용 시 로컬에 저장된 설명 사용
- “검색창에 맥북 입력해줘” → 원래 페이지 입력란 값이 맥북으로 변경, 제출 없음
- “구매 확정 버튼 눌러줘” → 확인 화면 표시. 취소 후 제출되지 않음
- 별도 확장 탭에서 원래 QA 페이지 연결 유지 및 실제 페이지 요약 반환
- 사용자 마이크 허용 후 “이 페이지 설명해줘”가 명령 기록에 나타남. 발견된 별도 탭 메시지 차단 버그 수정 후 같은 페이지 설명 경로의 AI 응답 재검증

- 일반 사이트 Wikipedia에서도 페이지 자동 분석과 “검색창에 웹 접근성 입력해줘” 명령의 실제 검색란 반영을 확인했습니다. 사이트별 하드코딩은 사용하지 않습니다.

## 범위와 제한

QA 페이지는 로컬 통합 검사 fixture이며 공식 Demo Page로 배포하거나 ZIP에 포함하지 않았습니다.

일반 사이트 전체, OS별 스크린리더, Chrome/OS별 마이크·음성 엔진을 모두 검증한 것은 아닙니다. 현재 Chrome에서는 Side Panel의 최초 마이크 권한 요청이 실패해 별도 확장 화면에서 허용하는 흐름을 확인했습니다. Voice Mode는 완전한 스크린리더 대체품이 아닙니다.

이미지 분석은 PNG/JPEG/WebP 등 브라우저가 디코딩 가능한 래스터 이미지를 우선 지원합니다. SVG 입력 실패를 확인했고 사용자 안내를 추가했습니다. AI 이미지 설명의 내용 정확성은 자동으로 보장되지 않습니다.

## 2026-09-09 사진·검색 수정 검증

- 기존 alt가 있는 사진도 AI 대상으로 포함하며 생성 후 원본 alt 복구, 바뀐 이미지 URL 거절을 검증했습니다.
- 이미지 READ가 기사 본문으로 넘어가지 않음을 검증했습니다.
- SEARCH는 검색 폼을 제출하고 TYPE은 입력만 한다는 회귀 테스트를 통과했습니다.
- Chrome 네이버 뉴스 /article/629/0000532093에서 사진설명해줘 명령으로 정비소 내부 사진에 대한 새 AI 설명이 반환되고 alt에 적용되는 것을 확인했습니다. 기존 BYD 사진 캡션과 기사 본문을 읽지 않았습니다.
- 원본 요청 실패 시 사진 영역 캡처 보완 경로를 추가했습니다. 모든 사이트의 차단 조건을 재현한 것은 아닙니다.

- 네이버 메인 광고 갱신에 의한 전체 revision 변경을 확인하고, SEARCH에 한해 동일 검색 폼·입력값·주소가 유지되는 경우 실행하도록 보완했습니다. 검색 폼 변경 시 거절하는 회귀 테스트를 추가했습니다.

- 최종 Chrome 실측: 네이버 메인에서 “검색창에 웹 접근성 입력해줘” → search.naver.com 검색 결과 URL(query=웹+접근성) 이동, “검색을 실행했습니다: 웹 접근성” 응답 확인.

## 기사 본문 사진 한정

- 네이버 본문, 일반 article, 광고·추천 제외 및 여러 본문 후보가 모호한 경우 분석 중단 테스트 통과.
- Chrome /article/629/0000532249?sid=103: 본문 사진 1장만 탐지. 전체 사진 명령은 1/1로 완료. 두 번째 사진 명령은 본문 사진 없음 안내. 관련 기사와 광고 사진으로 넘어가지 않음을 확인.
- 다른 언론사의 비표준 구조와 CSS 배경 이미지는 추가 대응이 필요할 수 있습니다.

## Google 검색·Chrome 새 탭

- 검색 textarea 지원, 입력만 유지, 검색 시 제출 및 별도 메시지 textarea를 포함한 폼 거절 테스트 통과.
- Chrome 새 탭 URL 인식, 한국어 검색어 추출과 비검색 명령 거절 테스트 통과.
- Chrome 실제 새 탭에서 “사과를 검색해줘” → google.com/search?q=사과 이동 및 결과 페이지 연결 확인. 새 탭 내부 DOM 주입은 사용하지 않습니다.

- Google 검색 폼의 빈 첨부 입력란은 허용하고 실제 첨부 파일이 있으면 거절합니다. 음성·이미지 검색 버튼을 제외하고 입력 후 나타나는 검색 버튼을 재검증합니다. 회귀 테스트 통과.
- 탭 주소를 Chrome이 제공하지 않는 경우 검색 준비 상태로 표시하고 새 Google 결과 탭을 열어 기존 탭을 보존합니다. 추가 브라우징 기록 권한은 요구하지 않습니다.

- 최종 Chrome 실측: Google 홈페이지에서 “사과를 검색해줘”로 기본 검색 폼 제출 및 google.com/search?q=사과 도착, “검색을 실행했습니다: 사과” 확인.
- 사이드패널을 열어 둔 채 Cmd+T: 추가 단축키 없이 검색 준비됨 표시, 사과 검색 명령으로 결과 탭 이동 및 이 페이지에 연결됨 확인.

## 동적 기사 페이지의 변경 판정

- 한겨레 기사에서 광고·추천 영역의 자동 갱신이 문서 전체 revision을 바꾸고, AI 응답을 기다리는 동안 같은 기사 명령까지 만료시키던 원인을 확인했습니다.
- “페이지 읽어줘”와 같은 명령은 AI 판단을 기다리지 않고 현재 기사 본문을 바로 읽도록 분리했습니다.
- 실행 전에는 문서 전체가 아니라 명령과 관련된 상태만 비교합니다. 페이지 읽기·요약은 본문, 클릭은 대상과 목적지, 입력은 입력란과 폼, 검색은 검색 폼과 검색어를 검증합니다.
- 한겨레의 `.article-text`에서 실제 기사 문단을 우선 추출하고 광고·추천·내비게이션 영역을 제외합니다.
- 광고 변경 후 읽기 허용, 본문 변경 후 읽기 거절, 추천 영역 변경 후 동일 버튼 읽기 허용, 링크 목적지 변경 후 클릭 거절 회귀 테스트를 통과했습니다.

## 단축키

- `Alt+A`(macOS: Option+A): 현재 탭의 Side Panel을 열고 마이크 입력 시작 요청을 전달합니다. 이미 열려 있는 Panel에는 즉시 전달하고, 처음 열리는 Panel에는 세션 저장소를 통해 마운트 후 전달합니다.
- `Alt+X`(macOS: Option+X): 현재 탭의 AccessAI Side Panel을 엽니다.
- production manifest에서 두 명령과 키 조합이 생성되는 것을 확인했습니다. 다른 확장프로그램 또는 Chrome 단축키와 충돌하면 Chrome이 단축키를 비워 둘 수 있으므로 `chrome://extensions/shortcuts`에서 재지정할 수 있습니다.

## 2026-09-12 Gemini 서버 지역 수정

- 기존 Worker 실행 위치는 홍콩(HKG)이었고, Gemini의 실제 응답은 HTTP 400 `FAILED_PRECONDITION` / `User location is not supported for the API use.`였습니다.
- 로컬에서 정상 응답을 반환하는 키로 Worker Secret을 다시 등록해도 같은 오류가 발생해 키 문제를 배제했습니다.
- 한국 리전 배치 힌트를 적용한 뒤 최종 배포에서 `cf-placement: remote-ICN`, 합성 클릭 명령 HTTP 200 / `CLICK`을 확인했습니다.
- 지역 제한 오류는 일반적인 일시 오류 대신 운영 설정 문제로 안내합니다. 상세 제공자 메시지는 사용자에게 노출하거나 상시 기록하지 않습니다.

## 2026-09-12 본문 시작 위치 수정

- 나무위키 실제 사과 페이지에서 본문은 링크와 줄바꿈이 섞인 `div`이고, `<p>`는 저작권·캡차 안내뿐임을 확인했습니다.
- 새 추출기를 실제 페이지에 실행해 반환값이 “사과나무의 열매.”로 시작하고 앞쪽 동음이의어 안내와 저작권 문구가 빠지는 것을 확인했습니다.
- 위키형 문단, 짧은 첫 정의, 불확실한 문단의 AI 시작 번호 선택, 기존 기사 본문 유지 회귀 테스트를 추가했습니다.
- 링크와 줄바꿈이 섞인 본문을 음성 조각과 원문 위치로 연결하고, 다음 조각을 읽을 때 화면 이동이 발생하는 DOM 테스트를 통과했습니다.
